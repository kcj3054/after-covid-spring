spring:
  datasource:
    url: jdbc:mysql://localhost:3306/afterCovid
    username: soomgo
    password: soomgo
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
#spring.jpa.show-sql: true